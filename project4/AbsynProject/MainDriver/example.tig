let
  var hd : int := 10
  type age = int
  var a := 20
  type teen = age
  var b : string := "abcd"
in
  hd := if 1>2 & 1/0 then a else b;
  b := b;
  print(b);
  print(1+2*10/2-9);
  while 1<=2 | 2 >= 3 do
    (a; b + "test"; hd);
  for i := 1 to 80*99 do
    let
	  var value : int := 0

	  function sum(i : int, n : int) : int =
	    (for t := i to n do
		   value := value + t;
		 value)

	  function printline() = 
	    print("\n")
	in
	  print( sum(1, 10) )
	end
end

